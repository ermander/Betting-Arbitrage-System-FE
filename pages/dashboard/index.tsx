import type { NextPage } from "next"
import style from "../../styles/Dashboard.module.scss"

const Dashboard: NextPage = () => {
  return <div id={`${style["dashboard-page"]}`}>Dashboard</div>
}

export default Dashboard
